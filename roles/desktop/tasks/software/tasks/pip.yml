---

- name: install python packages
  pip:
    name: "{{ item }}"
  with_items:
    - pipenv
    - virtualenv
    - docker-py
    - docker-compose
    - flake8
    - apache-libcloud
    - paramiko
    - PyYAML
    - jinja2
    - cryptography
    - httplib2
